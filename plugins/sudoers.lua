do

function run(msg, matches)
  return [[
#Sudoers!
  
  1.@The_DeSigN3r🌐 #Developer ( use #adddeveloper command to add #The_DeSigN3r in your Groups & SuperGroups. )
  
  2.@ParsaAlemi🌐 #Manager ( use #addmanager command to add #ParsaAlemi in your Groups & SuperGroups. )
  
]]
end
return {
  description = " ", 
  usage = " ",
  patterns = {
    "^[#!/]sudoers$",

  },
  run = run
}
end
